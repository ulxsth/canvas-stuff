import { Scene } from "./Scene";

export class Shape {
    public draw(
        x: number,
        y: number,
        angle: number,
        scale: number,
        deltaTime: number,
        scene: Scene
    ) {}
}